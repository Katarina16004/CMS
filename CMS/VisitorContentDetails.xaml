<Window x:Class="CMS.VisitorContentDetails"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CMS"
        mc:Ignorable="d"
        Background="Transparent"
        AllowsTransparency="True"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Title="ContentDetails">

    <Grid>
        <Viewbox Stretch="Fill">
            <Canvas Width="1000" Height="700">
                <Path Fill="#8971d0">
                    <Path.Data>
                        <PathGeometry>
                            <PathFigure StartPoint="20,0" IsClosed="True">
                                <LineSegment Point="980,0"/>
                                <QuadraticBezierSegment Point1="1000,0" Point2="1000,20"/>
                                <LineSegment Point="1000,680"/>
                                <QuadraticBezierSegment Point1="1000,700" Point2="980,700"/>
                                <LineSegment Point="20,700"/>
                                <QuadraticBezierSegment Point1="0,700" Point2="0,680"/>
                                <LineSegment Point="0,20"/>
                                <QuadraticBezierSegment Point1="0,0" Point2="20,0"/>
                            </PathFigure>
                        </PathGeometry>
                    </Path.Data>
                </Path>
            </Canvas>
        </Viewbox>

        <Border
            Background="White"
            CornerRadius="20"
            Margin="5" 
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch">

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Margin="0,0,0,10">

                    <TextBlock Grid.Column="1" x:Name="TitleTextBlock" Text="{Binding Text}" HorizontalAlignment="Center" FontSize="25"/>


                    <Image x:Name="ImagePreview" Grid.Column="1" Height="100" Width="100" Margin="20,0,0,0" Source="{Binding ImagePath}" HorizontalAlignment="Left" MouseLeftButtonDown="ImagePreview_MouseLeftButtonDown" Cursor="Hand"/>
                </StackPanel>
                <RichTextBox Grid.Row="1" x:Name="richTextBox" Margin="10,10,10,0" VerticalScrollBarVisibility="Auto" BorderThickness="0"  />

                <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" >
                    <Button Name="CloseButton" Content="Close" Style="{StaticResource ModernButton}" Click="CloseButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
